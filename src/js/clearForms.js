const clearValue = (inputTitle, inputAuthor) => {
    inputTitle.value = '';
    inputAuthor.value = '';
    // selectPriority.value = '';
    // selectCategory.value = '';
}

export default clearValue;